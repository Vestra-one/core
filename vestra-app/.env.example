# API base URL (no trailing slash). Used for all backend requests.
VITE_API_URL=http://localhost:3000/api

# Optional: set to "true" to use MSW mock API in development instead of real backend
# VITE_USE_MSW=true

# NEAR wallet: network (mainnet | testnet) and contract ID used for sign-in
# Default is mainnet. Unset uses mainnet + wrap.near for transfers.
# VITE_NEAR_NETWORK=mainnet
# VITE_NEAR_CONTRACT_ID=wrap.near
# Optional: RPC URL for balance
# VITE_NEAR_RPC_URL=https://rpc.mainnet.near.org

# For testnet (dev/testing only):
# VITE_NEAR_NETWORK=testnet
# VITE_NEAR_CONTRACT_ID=hello.near-examples.testnet
# 1Click API: JWT required for getQuote, submitDepositTx, getExecutionStatus. Request at https://partners.near-intents.org
# VITE_ONE_CLICK_JWT=your_jwt_token
# Optional: override 1Click API base (default https://1click.chaindefuser.com)
# VITE_ONE_CLICK_BASE=https://1click.chaindefuser.com

# NEP-366 gasless (meta) transactions: relayer URL and optional dev signer
# When set, payment send will use relayer (gasless) when a signer is available.
# VITE_RELAYER_URL=http://localhost:3031
# Dev-only: sign meta tx with this key (must match connected account for testing)
# VITE_DEV_META_TX_SIGNER_KEY=ed25519:...
# VITE_DEV_META_TX_SIGNER_ACCOUNT=your-account.testnet
