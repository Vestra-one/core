# API base URL (no trailing slash). Used for all backend requests.
VITE_API_URL=http://localhost:3000/api

# Optional: set to "true" to use MSW mock API in development instead of real backend
# VITE_USE_MSW=true

# NEAR wallet: network (testnet | mainnet) and contract ID used for sign-in
# VITE_NEAR_NETWORK=testnet
# VITE_NEAR_CONTRACT_ID=vestra.testnet   # required in prod; in dev, unset uses hello.near-examples.testnet
# Optional: RPC URL for balance (defaults to CORS-enabled FASTNEAR; set if you use another provider)
# VITE_NEAR_RPC_URL=https://rpc.testnet.fastnear.com

# NEAR Intents 1Click API (for intent-based cross-chain transfers). Get JWT from https://partners.near-intents.org
# VITE_ONE_CLICK_JWT=your_jwt_token
